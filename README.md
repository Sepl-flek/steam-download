# Steam Download Speed Monitor

Python-скрипт для фонового мониторинга скорости загрузки игр в **Steam** под **Windows**.  
Скрипт не зависит от пути установки Steam и использует системные данные и логи клиента.

---

## Возможности

- 📥 Определение текущей скорости загрузки игр в Steam  
- 🎮 Отображение названия игры, которая скачивается в данный момент  
- ⏸️ Учет статуса загрузки (активна / на паузе)  
- ⏱️ Вывод информации в консоль каждую минуту (в течение 5 минут)  
- 🔍 Автоматическое определение пути установки Steam через реестр Windows  

---

## Как работает

1. Путь установки Steam определяется через реестр Windows  
   `HKEY_CURRENT_USER\Software\Valve\Steam`

2. Статус загрузки и название игры извлекаются из логов:
   - `logs/content_log.txt`

3. Скорость загрузки вычисляется по входящему сетевому трафику процесса `steam.exe` с использованием библиотеки `psutil`.

> ⚠️ Steam не предоставляет публичного API для получения скорости загрузки,  
> поэтому используется анализ логов и сетевой активности процесса.

---

## Требования

- Windows 10 / 11
- Python 3.9+
- Установленный Steam

### Зависимости

```bash
pip install psutil pywin32
```

---

## Установка и запуск

Клонировать репозиторий:

```bash
git clone https://github.com/your-username/steam-download-monitor.git
cd steam-download-monitor
```

Установить зависимости:

```bash
pip install -r requirements.txt
```

Запустить скрипт:

```bash
python steam_download_monitor.py
```

---

## Пример вывода

```
[14:02:01] Минута 1/5
Игра: Cyberpunk 2077
Статус: downloading
Скорость: 11.42 MB/s
```

Если загрузка поставлена на паузу:

```
Статус: paused
Скорость: 0.00 MB/s
```

---

## Ограничения

- Скорость отображается как средняя за короткий интервал
- Название игры определяется на основе данных из логов Steam
- Поддерживается только Windows (из-за использования реестра)

---

## Возможные улучшения

- Экспорт данных в CSV / JSON
- Поддержка Linux и macOS
- Графический интерфейс (Tkinter / PySide)
- Запуск как системного сервиса
- Иконка в системном трее
